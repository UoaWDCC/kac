{
  "scripts": {
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && yarn run dev\"",
    "setup": "npm install && concurrently \"cd client && npm install\" \"cd server && npm install\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
